<h1>top_score_jeu creation</h1>

<form action="{{ path('top_score_jeu_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('top_score_jeu') }}">
            Back to the list
        </a>
    </li>
</ul>
