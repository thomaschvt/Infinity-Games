<h1>ArticlesFactory list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Titre</th>
            <th>Contenu</th>
            <th>Id_categorie</th>
            <th>Statut</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('articlesfactory_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.titre }}</td>
            <td>{{ entity.contenu }}</td>
            <td>{{ entity.idcategorie }}</td>
            <td>{{ entity.statut }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('articlesfactory_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('articlesfactory_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('articlesfactory_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
