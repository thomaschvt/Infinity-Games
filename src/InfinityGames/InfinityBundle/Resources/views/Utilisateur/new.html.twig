{% extends "::layout.html.twig" %}
	{% block contenu_page %}
<div class="span12">
	<div class="span3"></div>
	<div class="span6 bg_form ombre">
		<h1>Utilisateur creation</h1>
		
		<form action="{{ path('utilisateur_create') }}" method="post" {{ form_enctype(form) }}>
		    {{ form_widget(form) }}
		    <p>
		        <button class="btn" type="submit">S'inscire</button>
		    </p>
		</form>
	</div>
	<div class="span3"></div>
</div>
	{% endblock %}