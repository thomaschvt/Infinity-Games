<h1>MessageInterne list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Titremessage</th>
            <th>Contenumessage</th>
            <th>Date</th>
            <th>Heure</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('messageinterne_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.titreMessage }}</td>
            <td>{{ entity.contenuMessage }}</td>
            <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{% if entity.heure %}{{ entity.heure|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('messageinterne_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('messageinterne_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('messageinterne_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
